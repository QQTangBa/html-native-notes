# PRD Requirements And Acceptance Matrix

Date: 2026-06-27
PRD: `/Users/siter/Documents/需求池项目/research/ai-html-notes-prd.html`
Project root: `/Users/siter/Documents/HTML原生笔记编辑器/html-native-notes`

This matrix is the completion ledger. Rows cannot be marked complete without direct evidence from code, tests, screenshots, command output, or a launched desktop app run.

## Core Feature Matrix

| ID | PRD requirement | Implementation owner | Required tests | Current status | Evidence |
| --- | --- | --- | --- | --- | --- |
| F1 | Vault home with folder-like library, cards/list, thumbnails, title, tags, updated time, source, search, filters | React `features/vault`, Rust `core/vault` | component tests, desktop flow, 100-fixture search test | Minimal TS Vault Library data contract, API boundary, React VaultHome component, App loading path, browser runtime checks, Playwright thumbnail generation, toolbar trigger, and read-only preview started; Tauri command wiring, Rust Vault core, background thumbnail queue, and 100-fixture desktop flow missing | `npm run test -- tests/integration/bridge/vaultLibrary.test.ts` PASS for AI-generated HTML entering manifest and searchable/filterable metadata; `npm run test -- tests/integration/bridge/thumbnailGenerator.test.ts` PASS for real PNG thumbnail generation and source-hash preservation; `npm run test -- tests/integration/api.test.ts` PASS for `/api/vault/library`, `POST /api/vault/thumbnails/generate`, and `GET /api/vault/assets/:assetId/source`; `npm run test -- tests/unit/VaultHome.test.tsx` PASS for dense desktop Vault UI rendering, thumbnail action states, and read-only preview pane; `npm run test -- tests/unit/App.test.tsx` PASS for loading Vault assets, refreshing thumbnails, and opening HTML preview from the App shell; Playwright browser checks saved `test-results/desktop-launch/vault-home-runtime-html.png`, `test-results/desktop-launch/vault-thumbnail-action-pending.png`, `test-results/desktop-launch/vault-thumbnail-action-ready.png`, and `test-results/desktop-launch/vault-readonly-preview.png` |
| F2 | Agent Bridge through MCP, CLI, HTTP API, offline inbox, file watching | `bridge/*`, Rust inbox/watcher commands | MCP integration, HTTP integration, CLI integration, inbox startup, watcher test | Protocol schema, HTTP fallback, CLI fallback, offline inbox, watcher request generation, minimal manifest intake, TS thumbnail status update, and CLI/API thumbnail trigger started; MCP/desktop UI missing | `npm run test:bridge` PASS for shared request schemas, HTTP health, `register-html`, `register-service`, CLI `register`, CLI `service register`, CLI `thumbnail generate`, CLI `--offline-inbox`, JSONL read/dedupe/ack, watcher scan/new-file/dedupe, HTTP-to-manifest intake, source hash preservation, thumbnail generation contract, and validation errors |
| F3 | Service Registry with cwd/start/url/port/health/stop/env/log | Rust `service_registry`, React `features/services` | schema unit tests, UI tests, fixture service registration | Minimal TS registry, local App API, and Vault service card UI started; Rust core/native UI missing | `npm run test:bridge` PASS for service metadata persistence with cwd/start/url/port/health/stop/env/log; `npm run test -- tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for service health/start/stop API by Vault asset id and renderer card controls |
| F4 | Runtime Manager health-check, start if down, show state, stop | Rust `runtime_manager`, Tauri commands | fixture service start/stop, failed command log test | Minimal TS runtime plus App API/UI controls started; Rust/Tauri process boundary and native desktop verification missing | `npm run test:bridge` PASS for fixture service health/start/stop/restart and failed-start cwd/command/log evidence; `npm run test -- tests/integration/api.test.ts` PASS for starting/stopping a fixture local service through the main App API |
| F5 | Source Guard read-only intake and diff-gated writes | Rust `source_guard`, React `features/diff` | hash unchanged tests, path traversal tests, write gate UI test | Minimal TS intake hash guard, write-gate contract, Vault API write-review/decision routes, and React preview-source diff panel started; Rust Source Guard and native desktop flow missing | `npm run test:bridge` PASS for bridge intake hash check, source hash preservation, diff review, cancel, save-as, hash-checked write-back, and stale-source rejection; `npm run test -- tests/integration/api.test.ts` PASS for `POST /api/vault/assets/:assetId/write-review`, server-recomputed `POST /api/vault/write-decision`, realpath Vault boundary checks, symlink escape rejection, save-as collision rejection, and fabricated review isolation; `npm run test -- tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for preview HTML draft editing, Source Guard diff panel, cancel/save-as/write-back buttons, API review, cancel decision wiring, stale draft reset, and late-review race rejection |
| F6 | Version Engine snapshots, external changes, diff, rollback, branch/recover | Rust `version_store`, React `features/versions` | snapshot unit tests, external edit integration, rollback test | Minimal TS version store, local API routes, renderer API client, App wiring, and React timeline/diff/rollback UI started; Rust core, watcher-triggered external edit detection, branch/recover, and native desktop flow missing | `npm run test:bridge` PASS for baseline snapshots, source diff, content diff, DOM summary, and rollback of managed copy; `npm run test -- tests/unit/App.test.tsx tests/integration/bridge/versionStore.test.ts tests/integration/api.test.ts tests/unit/VaultHome.test.tsx` PASS for `/versions`, `/versions/snapshot`, `/versions/diff`, `/versions/rollback`, unsafe asset-id rejection, tampered snapshot content-path rejection, Version timeline UI, source/content/DOM diff panel, latest-version comparison, rollback refresh, and late diff race rejection |
| F7 | Markdown-first editor with shortcuts, slash command, wikilinks, backlinks, tags, quick open | React `features/editor` | editor command tests, backlink index tests, desktop edit flow | Minimal HTML note graph metadata started: saved HTML notes expose extracted `data-wikilink`, `data-tag`, visible `#tags`, and computed backlinks in NoteStore and NoteLibrary. Markdown-first rich editor, shortcuts, slash command, quick open, native desktop flow, and full backlink index still missing | `npm run test -- tests/unit/noteStore.test.ts tests/unit/NoteLibrary.test.tsx` PASS for wikilink/tag/backlink hydration and note list rendering; `npm run verify` PASS with 23 files / 99 tests |
| F8 | Markdown and existing import preserving frontmatter/images/wikilinks/tags/folders/code blocks/templates | Rust `scanner`, React `features/import` | 30 Markdown fixture test, 100 mixed fixture test, hash unchanged test | Minimal TS Markdown import contract started; Rust scanner, desktop import UI, 30-file fixture, and existing-project import missing | `npm run test:bridge` PASS for three templates, recursive Markdown folder scan with relative paths, frontmatter, images, wikilinks, and fenced code block conversion |
| F9 | HTML Profile with metadata, block IDs, asset list, AI context, theme vars, version | Rust `profile` | profile parse/write tests, migration tests | Minimal TS HTML Profile contract started; Rust profile core, sidecar manifest integration, and migration fixtures missing | `npm run test -- tests/unit/htmlProfile.test.ts` PASS for metadata, asset list, AI context, theme vars, block IDs, profile embedding/extraction, and legacy migration |
| F10 | Page-in-place editing from preview with snapshot + diff | React `features/preview`, Rust version/write gate | edit overlay test, diff gate desktop flow | Partial preview-source editing only: the user can edit the selected HTML source draft from the preview side panel and review a Source Guard diff; true page-in-place text-node overlay, snapshot-on-edit, and native desktop flow remain missing | `npm run test -- tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for preview-source draft edit and Source Guard review; no evidence yet for direct text-node editing inside the rendered page |
| F11 | Diary organization with at least two styles, preserves original | React `features/diary`, AI adapter | mocked AI test, live BYOK smoke test, original preservation test | Minimal BYOK AI diary organizer, local App API, renderer client, and compact Diary panel started; live 300-1000 word timing, persistent diary note creation, and native desktop flow missing | `npm run test -- tests/unit/diaryOrganizer.test.ts tests/integration/api.test.ts tests/unit/App.test.tsx` PASS for two-style JSON prompt/parse, caller-original preservation, `/api/diary/organize`, secret-safe response, configured renderer action, original-preserved UI, and insert-selected-style behavior |
| F12 | Open-source BYOK AI config, no hardcoded secrets | React `features/settings`, config command | config validation test, secret exposure test, live provider smoke | Safe BYOK config loading/status and renderer visibility started; in-app edit/persist flow, native secure storage, and live provider smoke still pending | `npm run test -- tests/unit/config.test.ts tests/integration/api.test.ts tests/unit/SettingsPanel.test.tsx` PASS for `.env.local` style config validation, safe provider status with base URL/model/temperature/max tokens/key-present flag, no secret exposure, and SettingsPanel rendering without API key value |
| F13 | Asset management for missing images/CSS/JS, dangerous scripts, broken externals, safe mode | Rust `asset_scan`, React `features/assets` | dangerous fixture test, missing asset test, report UI test | Minimal TS asset scanner, App API, and Vault card risk UI started; Rust core, localization/package repair flow, and native desktop flow missing | `npm run test:bridge` PASS for missing local images, external scripts/styles/links, dangerous inline scripts, unpublishable file URLs, safe mode flag, and source hash preservation; `npm run test -- tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for `/api/assets/:assetId/integrity`, read-only scan by Vault asset id, safe-mode summary, and renderer scan action |
| F14 | Export single file/folder package/Markdown/PDF and publish/static hosting adapter | Rust `exporter/publish`, React `features/publish` | export tests, package inspection, publish adapter test | Minimal local static package and Markdown export started; PDF, zip/package inspection beyond folder copy, provider publish adapter, Rust core, native save dialogs, and desktop flow missing | `npm run test -- tests/integration/bridge/exporter.test.ts tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for `.htmlvault/exports/<assetId>/latest/index.html`, local asset copy, manifest, external resource skip list, source hash preservation, Markdown export, App API routes, and Vault card export actions |
| F15 | macOS desktop app, Intel + Apple Silicon target, older macOS considered | Tauri `src-tauri` | desktop launch, bundle config check, build report | Scaffold started; not built | `npm run test -- tests/unit/desktopScaffold.test.ts` PASS; Rust toolchain still missing, so no desktop launch evidence yet |

## Acceptance Indicators

| ID | PRD acceptance indicator | Verification command or flow | Status | Evidence |
| --- | --- | --- | --- | --- |
| A1 | Import 100 mixed HTML/Markdown files; 95% open/search/preview | `npm run test:fixtures:mixed100` plus desktop import screenshot | Partial | Single AI-generated HTML fixture can enter Vault Library, be searched/filtered, and open in a read-only preview pane; 100 mixed fixture and native desktop screenshot pending |
| A2 | MCP/CLI/HTTP/file-watch generated HTML enters Vault within 10 seconds with source, summary, thumbnail | `npm run test:bridge` and desktop bridge flow | Partial | HTTP-to-manifest intake exists; CLI/API and the current renderer toolbar can trigger TS thumbnail generation and update Vault Library status to ready; MCP, native desktop UI, summary quality, and 10-second desktop flow still pending |
| A3 | Offline inbox fallback works when MCP/HTTP/CLI unavailable and app later prompts registration | inbox JSONL integration test and desktop startup flow | Missing | Pending |
| A4 | Registered local dashboard health-checks, starts, and shows command/cwd/log on failure | fixture service desktop flow | Partial | TS runtime fixture starts and failed startup returns cwd/command/log; main App API can health-check/start/stop by Vault asset id, and Vault service cards show cwd/status/actions. Native desktop UI flow pending |
| A5 | User can stop app-started service and restart it | service lifecycle integration test | Partial | TS runtime stops and restarts app-managed fixture process; main App API and renderer controls can stop a service. Native desktop verification pending |
| A6 | Intake original HTML preserves content hash | source hash before/after test | Partial | bridge intake test preserves source hash; Rust Source Guard and desktop import evidence still pending |
| A7 | Any write back shows diff and supports write back/save-as/cancel | diff gate component + desktop flow | Partial | TS write gate returns readable diff and enforces cancel/save-as/write-back decisions with source hash checks; Vault API exposes review/decision routes, recomputes review server-side, rejects symlink escapes, and refuses save-as overwrite; React preview panel shows the diff and explicit cancel/save-as/write-back buttons. Native desktop verification and Rust Source Guard remain pending |
| A8 | Import 100 old web/project dirs without modifying originals | project fixture hash test | Missing | Pending |
| A9 | External agent edits create snapshots; user can diff and roll back | watcher + version integration test | Partial | Version snapshot API can record an external-agent-edit reason, UI can diff latest snapshots, and rollback refreshes the preview; automatic watcher-to-version snapshot flow pending |
| A10 | Two HTML versions show source diff, content diff, DOM summary, screenshot hint | version diff UI flow | Partial | React Version diff panel shows source diff, readable content diff, DOM added tags, and changed title; screenshot hint and native desktop visual flow pending |
| A11 | 300-1000 word diary generates two organization styles within 30 seconds and preserves original | mocked AI timing test + BYOK smoke | Partial | Mocked App/API flow now returns timeline and themes styles while preserving the caller original and rendering an original-preserved review area; live DeepSeek timing, 300-1000 word fixture, and native desktop verification pending |
| A12 | Preview edit button allows direct text edit, then diff/write/save-as/cancel | desktop preview edit flow | Partial | Preview-source edit button now opens an editable HTML draft, reviews changes through Source Guard, exposes cancel/save-as/write-back decisions in React, resets stale drafts on preview switch, and ignores late reviews from prior assets. Direct text editing inside the rendered page and native desktop flow remain pending |
| A13 | Local HTML upload generates public link reachable outside local network | publish adapter test with configured target | Missing | Local static package export exists, but no provider upload/link generation has been implemented; pending provider decision |
| A14 | Import 30 Markdown notes preserving wikilinks/images/frontmatter/folders/code blocks and 3 templates | `npm run test:fixtures:markdown30` | Partial | Initial TS contract covers 3 templates plus one-file preservation of wikilinks/images/frontmatter/folders/code blocks; 30-note fixture and desktop import flow pending |
| A15 | User can create/edit HTML notes with Markdown syntax and `[[page]]` backlinks | editor and backlink tests | Partial | Saved HTML notes can surface `data-wikilink` and computed backlinks in the library; direct Markdown syntax editing and `[[page]]` creation workflow still pending |
| A16 | Asset integrity reports missing images, external scripts, unpublishable resources | asset scanner dangerous fixture test | Partial | TS asset scanner reports missing image, external script/style/link, dangerous inline script, unpublishable file URL; main App API and Vault card UI can show missing/external/script/blocked counts. Native desktop asset panel and repair/package flow pending |
| A17 | 3-minute flow: AI page enters Vault, diff visible, Markdown-style edit, export | end-to-end timed desktop script | Partial | The individual web/App API pieces now exist for Vault intake, preview, diff-gated edit, Markdown-style metadata, and local package/Markdown export. Timed native desktop flow remains pending |

## Required Evidence Naming

When implementation starts, evidence should be saved under:

```text
test-results/
  desktop-launch/
  fixture-imports/
  bridge/
  source-guard/
  versions/
  services/
  editor/
  assets/
  publish/
```

Each acceptance row should receive:

- command run
- pass/fail output
- relevant screenshots
- fixture path
- source hash proof when Source Guard is involved
- date/time of run

## Current Gate

The project has moved from pure web prototype to a minimal Tauri scaffold, but cannot build or launch the desktop app on this machine until the Tauri environment decision is made:

- Option 1: user approves Rust toolchain installation for Tauri development.
- Option 2: user explicitly chooses Electron fallback, accepting higher package size and resource cost.

The PRD recommends Tauri, so Option 1 is the default technical recommendation.
