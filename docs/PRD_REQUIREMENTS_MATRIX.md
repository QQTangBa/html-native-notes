# PRD Requirements And Acceptance Matrix

Date: 2026-06-27
PRD: `/Users/siter/Documents/需求池项目/research/ai-html-notes-prd.html`
Project root: `/Users/siter/Documents/HTML原生笔记编辑器/html-native-notes`

This matrix is the completion ledger. Rows cannot be marked complete without direct evidence from code, tests, screenshots, command output, or a launched desktop app run.

## Latest Incremental Evidence

Date: 2026-06-27

- Markdown takeover advanced: `registerMarkdownAsset` now stores `markdown-note` assets, preserves the original Markdown hash, exposes rendered HTML preview, indexes frontmatter/tags/wikilinks, and supports conversion into a new managed `html-note` without mutating the `.md` source.
- Rendered-page editing advanced: the preview panel can list editable rendered HTML elements by stable selector, review selected-element text changes through Source Guard, and build scoped AI edit requests using `selection` metadata.
- Browser smoke evidence exists under `test-results/desktop-smoke/`: `vault-element-editor-review.png` and `vault-markdown-converted-preview.png`.
- Live BYOK provider smoke passed through `node scripts/test-ai-config.mjs` against DeepSeek `deepseek-v4-flash`; the API key stayed in ignored `.env.local`.

## Core Feature Matrix

| ID | PRD requirement | Implementation owner | Required tests | Current status | Evidence |
| --- | --- | --- | --- | --- | --- |
| F1 | Vault home with folder-like library, cards/list, thumbnails, title, tags, updated time, source, search, filters | React `features/vault`, Rust `core/vault` | component tests, desktop flow, 100-fixture search test | Minimal TS Vault Library data contract, API boundary, React VaultHome component, App loading path, browser runtime checks, Playwright thumbnail generation, toolbar trigger, read-only preview, renderer `desktopBridge` command boundary, and native Rust Vault list/source command source started; Cargo verification, background native thumbnail queue, and 100-fixture launched desktop flow missing | `npm run test -- tests/integration/bridge/vaultLibrary.test.ts` PASS for AI-generated HTML entering manifest and searchable/filterable metadata; `npm run test -- tests/integration/bridge/thumbnailGenerator.test.ts` PASS for real PNG thumbnail generation and source-hash preservation; `npm run test -- tests/integration/api.test.ts` PASS for `/api/vault/library`, `POST /api/vault/thumbnails/generate`, and `GET /api/vault/assets/:assetId/source`; `npm run test -- tests/unit/VaultHome.test.tsx` PASS for dense desktop Vault UI rendering, thumbnail action states, and read-only preview pane; `npm run test -- tests/unit/App.test.tsx` PASS for loading Vault assets, refreshing thumbnails, opening HTML preview from the App shell, and loading Vault items through `desktopBridge` when Tauri invoke is available; `npm run test -- tests/unit/desktopBridge.test.ts` PASS for renderer command names and fallback behavior; `npm run test:desktop:contract` PASS for native `vault_list_library` and `vault_get_asset_source` source/registration contract; Playwright browser checks saved `test-results/desktop-launch/vault-home-runtime-html.png`, `test-results/desktop-launch/vault-thumbnail-action-pending.png`, `test-results/desktop-launch/vault-thumbnail-action-ready.png`, and `test-results/desktop-launch/vault-readonly-preview.png` |
| F2 | Agent Bridge through MCP, CLI, HTTP API, offline inbox, file watching | `bridge/*`, Rust inbox/watcher commands | MCP integration, HTTP integration, CLI integration, inbox startup, watcher test | Protocol schema, MCP tool definitions/local dispatcher, HTTP fallback, CLI fallback, offline inbox, watcher request generation, minimal manifest intake, TS thumbnail status update, CLI/API thumbnail trigger, React Agent Inbox review UI, local API inbox confirm/dismiss, renderer desktopBridge commands, App startup inbox loading, and native Rust inbox command source started; official MCP SDK stdio transport, Cargo verification, and launched Tauri inbox flow missing | `npm run test:bridge` PASS for shared request schemas, MCP V1 tool definitions and local calls for register/search/create/snapshot/publish/import, HTTP health, `register-html`, `register-service`, CLI `register`, CLI `service register`, CLI `thumbnail generate`, CLI `--offline-inbox`, JSONL read/dedupe/ack, watcher scan/new-file/dedupe, HTTP-to-manifest intake, source hash preservation, thumbnail generation contract, and validation errors; `npm run test -- tests/unit/InboxPanel.test.tsx tests/unit/VaultHome.test.tsx` PASS for pending inbox requests, duplicate and invalid-line counters, busy/empty states, and Vault sidebar confirm/dismiss event forwarding; `npm run test -- tests/unit/desktopBridge.test.ts tests/integration/api.test.ts tests/unit/App.test.tsx` PASS for `inbox_list_requests`, `inbox_confirm_request`, `inbox_dismiss_request`, local `/api/agent/inbox`, confirm-to-Vault intake, dismiss ack, and App startup rendering; `npm run test:desktop:contract` PASS for native Rust inbox source/command registration contract |
| F3 | Service Registry with cwd/start/url/port/health/stop/env/log | Rust `service_registry`, React `features/services` | schema unit tests, UI tests, fixture service registration | Minimal TS registry, local App API, Vault service card UI, and native Rust service runtime command source started; Cargo verification and native desktop UI flow missing | `npm run test:bridge` PASS for service metadata persistence with cwd/start/url/port/health/stop/env/log; `npm run test -- tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for service health/start/stop API by Vault asset id and renderer card controls; `npm run test:desktop:contract` PASS for native `service_check_health`, `service_start`, and `service_stop` source/registration contract, registry-backed service asset mapping, cwd/start/stop/url/health/env/log fields, `/bin/zsh` command boundary, local TCP health probe, app-managed process id tracking, and failure status/log-path evidence |
| F4 | Runtime Manager health-check, start if down, show state, stop | Rust `runtime_manager`, Tauri commands | fixture service start/stop, failed command log test | Minimal TS runtime, App API/UI controls, and native Rust service runtime command source started; Cargo verification and native desktop verification missing | `npm run test:bridge` PASS for fixture service health/start/stop/restart and failed-start cwd/command/log evidence; `npm run test -- tests/integration/api.test.ts` PASS for starting/stopping a fixture local service through the main App API; `npm run test:desktop:contract` PASS for native service health/start/stop command source, runtime status shape, app-managed process tracking, and log-path failure evidence |
| F5 | Source Guard read-only intake and diff-gated writes | Rust `source_guard`, React `features/diff` | hash unchanged tests, path traversal tests, write gate UI test | Minimal TS intake hash guard, write-gate contract, Vault API write-review/decision routes, React preview-source diff panel, and native Rust Source Guard command source started; Cargo verification and native desktop flow missing | `npm run test:bridge` PASS for bridge intake hash check, source hash preservation, diff review, cancel, save-as, hash-checked write-back, and stale-source rejection; `npm run test -- tests/integration/api.test.ts` PASS for `POST /api/vault/assets/:assetId/write-review`, server-recomputed `POST /api/vault/write-decision`, realpath Vault boundary checks, symlink escape rejection, save-as collision rejection, and fabricated review isolation; `npm run test -- tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for preview HTML draft editing, Source Guard diff panel, cancel/save-as/write-back buttons, API review, cancel decision wiring, stale draft reset, and late-review race rejection; `npm run test:desktop:contract` PASS for native `source_guard_review_write` and `source_guard_apply_write_decision` source/registration contract, readable diff, explicit cancel/save-as/write-back decisions, source hash check, and canonical Vault boundary checks for source/save-as paths |
| F6 | Version Engine snapshots, external changes, diff, rollback, branch/recover | Rust `version_store`, React `features/versions` | snapshot unit tests, external edit integration, rollback test | Minimal TS version store, debounced external edit watcher, local API routes, renderer API client, App wiring, React timeline/diff/rollback UI, and native Rust Version Engine command source started; Cargo verification, native watcher, branch/recover, screenshot hints, and native desktop flow missing | `npm run test:bridge` PASS for baseline snapshots, source diff, content diff, DOM summary, and rollback of managed copy; `npm run test -- tests/unit/App.test.tsx tests/integration/bridge/versionStore.test.ts tests/integration/bridge/externalEditVersion.test.ts tests/integration/api.test.ts tests/unit/VaultHome.test.tsx` PASS for `/versions`, `/versions/snapshot`, `/versions/diff`, `/versions/rollback`, one-shot `external-agent-edit` snapshot creation after source hash change, debounced watcher-triggered external edit snapshot creation, unsafe asset-id rejection, tampered snapshot content-path rejection, Version timeline UI, source/content/DOM diff panel, latest-version comparison, rollback refresh, and late diff race rejection; `npm run test:desktop:contract` PASS for native `vault_list_versions`, `vault_create_version_snapshot`, `vault_diff_versions`, and `vault_rollback_version` source/registration contract, safe asset id checks, snapshot content realpath boundary checks, source path Vault boundary checks, readable source/content diff, DOM summary, and rollback response shape |
| F7 | Markdown-first editor with shortcuts, slash command, wikilinks, backlinks, tags, quick open | React `features/editor` | editor command tests, backlink index tests, desktop edit flow | Minimal HTML note graph metadata, TS note store, NoteLibrary rendering, and native Rust note command source started. Markdown-first rich editor, shortcuts, slash command, quick open, native desktop flow, and full backlink index remain missing | `npm run test -- tests/unit/noteStore.test.ts tests/unit/NoteLibrary.test.tsx` PASS for wikilink/tag/backlink hydration and note list rendering; `npm run test:desktop:contract` PASS for native `note_list`, `note_create`, `note_get`, `note_save_content`, `note_duplicate`, and `note_delete` source/registration contract, local `.htmlvault/notes-store` metadata/notes/trash storage, safe note id checks, and wikilink/tag/backlink metadata hooks |
| F8 | Markdown and existing import preserving frontmatter/images/wikilinks/tags/folders/code blocks/templates | Rust `scanner`, React `features/import` | 30 Markdown fixture test, 100 mixed fixture test, hash unchanged test | Minimal TS Markdown import contract, deterministic PRD fixture generator, and native Rust import scanner command source started; desktop import UI, full 95/100 open-search-preview run, Cargo verification, and launched native import flow missing | `npm run test:bridge` PASS for three templates, recursive Markdown folder scan with relative paths, frontmatter, images, wikilinks, and fenced code block conversion; `npm run test -- tests/integration/fixtures/prdFixtures.test.ts` PASS for generating 100 mixed fixtures, converting 30 Markdown notes without source mutation, and registering 70 importable HTML/project index files into the TS Vault library; `npm run test:desktop:contract` PASS for native `import_scan_candidates` source/registration contract, read-only HTML/Markdown/project/service candidate scanning, `package.json` and `index.html` detection, source hash calculation, and skipping `.git`, `.htmlvault`, `node_modules`, `dist`, `build`, `target`, `.next`, and `.turbo` |
| F9 | HTML Profile with metadata, block IDs, asset list, AI context, theme vars, version | Rust `profile` | profile parse/write tests, migration tests | Minimal TS HTML Profile contract and native Rust profile command source started; sidecar manifest integration, Cargo verification, launched editor persistence, and migration fixtures missing | `npm run test -- tests/unit/htmlProfile.test.ts` PASS for metadata, asset list, AI context, theme vars, block IDs, profile embedding/extraction, and legacy migration; `npm run test:desktop:contract` PASS for native `profile_build`, `profile_embed`, `profile_extract`, and `profile_migrate` source/registration contract, metadata/source hash fields, assets, blocks, AI context, theme vars, `data-ainote-block-id`, and embedded `ainote-profile` handling |
| F10 | Page-in-place editing from preview with snapshot + diff | React `features/preview`, Rust version/write gate | edit overlay test, diff gate desktop flow | Partial preview-source editing only: the user can edit the selected HTML source draft from the preview side panel and review a Source Guard diff; true page-in-place text-node overlay, snapshot-on-edit, and native desktop flow remain missing | `npm run test -- tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for preview-source draft edit and Source Guard review; no evidence yet for direct text-node editing inside the rendered page |
| F11 | Diary organization with at least two styles, preserves original | React `features/diary`, AI adapter | mocked AI test, live BYOK smoke test, original preservation test | Minimal BYOK AI diary organizer, local App API, renderer client, and compact Diary panel started; live 300-1000 word timing, persistent diary note creation, and native desktop flow missing | `npm run test -- tests/unit/diaryOrganizer.test.ts tests/integration/api.test.ts tests/unit/App.test.tsx` PASS for two-style JSON prompt/parse, caller-original preservation, `/api/diary/organize`, secret-safe response, configured renderer action, original-preserved UI, and insert-selected-style behavior |
| F12 | Open-source BYOK AI config, no hardcoded secrets | React `features/settings`, config command | config validation test, secret exposure test, live provider smoke | Safe BYOK config loading/status and renderer visibility started; in-app edit/persist flow, native secure storage, and live provider smoke still pending | `npm run test -- tests/unit/config.test.ts tests/integration/api.test.ts tests/unit/SettingsPanel.test.tsx` PASS for `.env.local` style config validation, safe provider status with base URL/model/temperature/max tokens/key-present flag, no secret exposure, and SettingsPanel rendering without API key value |
| F13 | Asset management for missing images/CSS/JS, dangerous scripts, broken externals, safe mode | Rust `asset_scan`, React `features/assets` | dangerous fixture test, missing asset test, report UI test | Minimal TS asset scanner, App API, Vault card risk UI, and native Rust asset scan command source started; Cargo verification, localization/package repair flow, and native desktop flow missing | `npm run test:bridge` PASS for missing local images, external scripts/styles/links, dangerous inline scripts, unpublishable file URLs, safe mode flag, and source hash preservation; `npm run test -- tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for `/api/assets/:assetId/integrity`, read-only scan by Vault asset id, safe-mode summary, and renderer scan action; `npm run test:desktop:contract` PASS for native `asset_scan_integrity` source/registration contract, read-only scanner source, source hash reporting, dangerous inline script detection, Vault boundary check, and no `fs::write` in the scanner core |
| F14 | Export single file/folder package/Markdown/PDF and publish/static hosting adapter | Rust `exporter/publish`, React `features/publish` | export tests, package inspection, publish adapter test | Minimal local static package, Markdown export, command-provider static publish adapter, native Rust export command source, and native Rust publish command source started; PDF, zip/package inspection beyond folder copy, real provider upload proof, native save dialogs, publish UI, and desktop flow missing | `npm run test -- tests/integration/bridge/exporter.test.ts tests/integration/api.test.ts tests/unit/VaultHome.test.tsx tests/unit/App.test.tsx` PASS for `.htmlvault/exports/<assetId>/latest/index.html`, local asset copy, manifest, external resource skip list, source hash preservation, Markdown export, App API routes, and Vault card export actions; `npm run test -- tests/integration/bridge/publish.test.ts tests/integration/api.test.ts tests/unit/config.test.ts tests/unit/desktopBridge.test.ts` PASS for provider-neutral static package publish, command-provider URL parsing, disabled-provider rejection, secret redaction, env config parsing, `/api/publish/:assetId/static`, and renderer bridge mapping; `npm run test:desktop:contract` PASS for native `export_static_package`, `export_markdown`, and `publish_static` source/registration contracts, managed `.htmlvault/exports/<assetId>/latest` package output, local asset copy helpers, skipped external resources, manifest writing, Markdown conversion helper, user-configured publish env, provider command execution without shell, HTTP(S) public URL parsing, and redacted provider command failures |
| F15 | macOS desktop app, Intel + Apple Silicon target, older macOS considered | Tauri `src-tauri` | desktop launch, bundle config check, build report | Scaffold, renderer bridge boundary, native inbox command source, native Vault list/source command source, native Source Guard command source, native Version Engine command source, native importer command source, native HTML Profile command source, native asset scan command source, native service runtime command source, native export command source, native publish command source, and native note command source started; Rust toolchain still missing, so app not built/opened | `npm run test:desktop:contract` PASS for Tauri macOS bundle config, desktop scripts, `app_health`, native inbox command registration source, native Vault list/source command registration source, native Source Guard command registration source, native Version Engine command registration source, native importer command registration source, native HTML Profile command registration source, native asset scan command registration source, native service runtime command registration source, native export command registration source, native publish command registration source, and native note command registration source; `npm run test -- tests/unit/desktopBridge.test.ts tests/unit/App.test.tsx` PASS for renderer-to-Tauri invoke boundary and transitional fallback; Rust toolchain still missing, so no Cargo build or desktop launch evidence yet |

## Acceptance Indicators

| ID | PRD acceptance indicator | Verification command or flow | Status | Evidence |
| --- | --- | --- | --- | --- |
| A1 | Import 100 mixed HTML/Markdown files; 95% open/search/preview | `npm run test:fixtures:mixed100` plus desktop import screenshot | Partial | Deterministic 100-file mixed fixture generation exists and 70 importable HTML/project index files can register into the TS Vault library with hashes preserved; full 95/100 open/search/preview and native desktop screenshot pending |
| A2 | MCP/CLI/HTTP/file-watch generated HTML enters Vault within 10 seconds with source, summary, thumbnail | `npm run test:bridge` and desktop bridge flow | Partial | MCP local tool dispatcher, HTTP-to-manifest intake, CLI/API thumbnail trigger, and file-watch request generation exist; current renderer toolbar can update Vault Library status to ready. Official MCP stdio transport, native desktop UI, summary quality, and 10-second desktop flow still pending |
| A3 | Offline inbox fallback works when MCP/HTTP/CLI unavailable and app later prompts registration | inbox JSONL integration test and desktop startup flow | Partial | JSONL offline inbox append/read/dedupe/ack tests exist through `npm run test:bridge`; the React Vault sidebar renders a review panel with confirm/dismiss callbacks; local API reads Vault `.htmlvault/inbox/requests.jsonl`, confirms by intaking into Vault then acking, dismisses by acking, and App startup renders pending inbox items. Rust source now includes native app-support + Vault inbox path merge and command registration, but Cargo build and launched Tauri prompt flow remain pending |
| A4 | Registered local dashboard health-checks, starts, and shows command/cwd/log on failure | fixture service desktop flow | Partial | TS runtime fixture starts and failed startup returns cwd/command/log; main App API can health-check/start/stop by Vault asset id, and Vault service cards show cwd/status/actions. Native service runtime command source now mirrors cwd/command/log/status fields, but native desktop UI flow and Cargo verification remain pending |
| A5 | User can stop app-started service and restart it | service lifecycle integration test | Partial | TS runtime stops and restarts app-managed fixture process; main App API and renderer controls can stop a service. Native service runtime command source now includes app-managed process id tracking and stop command fallback, but native desktop verification remains pending |
| A6 | Intake original HTML preserves content hash | source hash before/after test | Partial | bridge intake test preserves source hash; native Source Guard command source now hashes source files before write-back, but desktop import evidence and Cargo verification are still pending |
| A7 | Any write back shows diff and supports write back/save-as/cancel | diff gate component + desktop flow | Partial | TS write gate returns readable diff and enforces cancel/save-as/write-back decisions with source hash checks; Vault API exposes review/decision routes, recomputes review server-side, rejects symlink escapes, and refuses save-as overwrite; React preview panel shows the diff and explicit cancel/save-as/write-back buttons. Native Rust Source Guard command source now exposes review/apply commands and canonical Vault path checks; native desktop verification and Cargo build remain pending |
| A8 | Import 100 old web/project dirs without modifying originals | project fixture hash test | Partial native scanner source | Native `import_scan_candidates` source covers read-only candidate scanning and hash reporting, and TS fixtures cover source-hash preservation for generated HTML/Markdown samples. Full 100 old project-dir import, before/after hash report, desktop import UI, and launched app evidence remain pending |
| A9 | External agent edits create snapshots; user can diff and roll back | watcher + version integration test | Partial | TS debounced external edit watcher can create exactly one `external-agent-edit` snapshot after a registered HTML source hash changes; Version snapshot API can record the reason, UI can diff latest snapshots, and rollback refreshes the preview. Native Version Engine command source now covers snapshot/diff/rollback boundaries, but native desktop watcher/UI flow and screenshot evidence remain pending |
| A10 | Two HTML versions show source diff, content diff, DOM summary, screenshot hint | version diff UI flow | Partial | React Version diff panel shows source diff, readable content diff, DOM added tags, and changed title; native Version Engine command source now includes source/content diff and DOM summary functions. Screenshot hint and native desktop visual flow remain pending |
| A11 | 300-1000 word diary generates two organization styles within 30 seconds and preserves original | mocked AI timing test + BYOK smoke | Partial | Mocked App/API flow now returns timeline and themes styles while preserving the caller original and rendering an original-preserved review area; live DeepSeek timing, 300-1000 word fixture, and native desktop verification pending |
| A12 | Preview edit button allows direct text edit, then diff/write/save-as/cancel | desktop preview edit flow | Partial | Preview-source edit button now opens an editable HTML draft, reviews changes through Source Guard, exposes cancel/save-as/write-back decisions in React, resets stale drafts on preview switch, and ignores late reviews from prior assets. Direct text editing inside the rendered page and native desktop flow remain pending |
| A13 | Local HTML upload generates public link reachable outside local network | publish adapter test with configured target | Partial | Command-provider publish adapter can package a Vault HTML asset and accept a provider-returned HTTP(S) URL while redacting secrets; real approved provider credentials and outside-network reachability test remain pending |
| A14 | Import 30 Markdown notes preserving wikilinks/images/frontmatter/folders/code blocks and 3 templates | `npm run test:fixtures:markdown30` | Partial | Deterministic 30-note Markdown fixture suite now covers frontmatter tags, wikilinks, images, folders, and code blocks without mutating source notes; desktop import UI and native app screenshot pending |
| A15 | User can create/edit HTML notes with Markdown syntax and `[[page]]` backlinks | editor and backlink tests | Partial | Saved HTML notes can surface `data-wikilink` and computed backlinks in the library; native note command source now covers local HTML note CRUD and graph metadata hooks. Direct Markdown syntax editing, `[[page]]` creation workflow, and launched desktop flow remain pending |
| A16 | Asset integrity reports missing images, external scripts, unpublishable resources | asset scanner dangerous fixture test | Partial | TS asset scanner reports missing image, external script/style/link, dangerous inline script, unpublishable file URL; main App API and Vault card UI can show missing/external/script/blocked counts. Native asset scan command source now mirrors the read-only report boundary; native desktop asset panel and repair/package flow remain pending |
| A17 | 3-minute flow: AI page enters Vault, diff visible, Markdown-style edit, export | end-to-end timed desktop script | Partial | The individual web/App API pieces now exist for Vault intake, preview, diff-gated edit, Markdown-style metadata, and local package/Markdown export. Native export command source is started, but timed native desktop flow remains pending |

## Required Evidence Naming

When implementation starts, evidence should be saved under:

```text
test-results/
  desktop-launch/
  fixture-imports/
  bridge/
  source-guard/
  versions/
  services/
  editor/
  assets/
  publish/
```

Each acceptance row should receive:

- command run
- pass/fail output
- relevant screenshots
- fixture path
- source hash proof when Source Guard is involved
- date/time of run

## Current Gate

The project has moved from pure web prototype to a minimal Tauri scaffold, but cannot build or launch the desktop app on this machine until the Tauri environment decision is made:

- Option 1: user approves Rust toolchain installation for Tauri development.
- Option 2: user explicitly chooses Electron fallback, accepting higher package size and resource cost.

The PRD recommends Tauri, so Option 1 is the default technical recommendation.
