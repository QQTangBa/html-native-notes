{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for the HTML Native Notes shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
